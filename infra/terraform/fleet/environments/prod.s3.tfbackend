# Sample backend config for the prod fleet stack. Copy the bucket in and pass at
# init: terraform init -backend-config=environments/prod.s3.tfbackend
bucket       = "CHANGEME-rp2-tfstate"
key          = "rp2/fleet-prod.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
