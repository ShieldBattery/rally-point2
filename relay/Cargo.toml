[package]
name = "rally-point-relay"
description = "Validating netcode v2 relay: command validation, mesh + dedup, per-turn consensus, replicated turn log, and flight recorder."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
rally-point-proto = { workspace = true }
rally-point-transport = { workspace = true }
parking_lot = { workspace = true }
clap = { workspace = true }
color-eyre = { workspace = true }
ring = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[dev-dependencies]
# Self-signed relay cert for the loopback client-edge test. The QUIC + Ed25519
# crypto in the test itself comes from the transport re-exports (quinn/rustls)
# and the `ring` dependency above.
rcgen = { workspace = true }
